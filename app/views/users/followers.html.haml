.main
  .main-left
    = link_to "トップへ戻る", root_path
    |
    = link_to "#{@user.nickname}さんがフォローしているアカウントを見る", following_user_path(@user)
    .tweet-home
      #{@user.nickname}さんは#{@user.followed.count}人にフォローされています
    = render partial: "users/user", collection: @users
  .main-right
    = render partial: "layouts/profile", locals: {user: @user}


