.tweet
  .tweet__body
    = link_to user.nickname, user_path(user)
    @#{user.username}
  .tweet__time
    = user.tweets.last.try(:body)
  .tweet__follow-button
    = render partial: "users/follow_form", locals: {user: user} if current_user
